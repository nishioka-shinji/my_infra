apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ztunnel
  namespace: istio-system
spec:
  interval: 1h
  timeout: 20m
  chart:
    spec:
      chart: ztunnel
      version: '1.29.2'
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: istio-system
