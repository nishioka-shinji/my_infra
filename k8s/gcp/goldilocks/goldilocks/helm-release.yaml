apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: goldilocks
  namespace: goldilocks
spec:
  interval: 30m
  chart:
    spec:
      chart: goldilocks
      version: "10.1.0"
      sourceRef:
        kind: HelmRepository
        name: fairwinds-stable
  values:
    controller:
      resources:
        requests:
          cpu: 1m
          memory: 21M
        limits:
          cpu: 2m
          memory: 26M
    dashboard:
      replicaCount: 1
      resources:
        requests:
          cpu: 1m
          memory: 8389k
        limits:
          cpu: 2m
          memory: 28M
